import {useEffect,useState} from 'react'; import {Users,School,TrendingUp,UserRound} from 'lucide-react'; import {dashboardAPI} from '../utils/api';
export default function Dashboard(){const [s,setS]=useState(null),[err,setErr]=useState('');useEffect(()=>{dashboardAPI.getStats().then(r=>setS(r.data)).catch(()=>setErr('Could not load dashboard. Check the backend and database.'))},[]);if(err)return <div className="alert error-box">{err}</div>;if(!s)return <div className="loading">Loading dashboard...</div>;const cards=[['Students',s.summary.totalStudents,Users],['Classes',s.summary.totalClasses,School],['Average score',s.summary.averageScore+'%',TrendingUp],['Teachers',s.summary.totalTeachers,UserRound]];return <div className="page"><div className="page-head"><div><h1>Dashboard</h1><p>Overview of your school data.</p></div></div><div className="stats">{cards.map(([l,v,I])=><div className="stat" key={l}><div><span>{l}</span><strong>{v}</strong></div><I size={28}/></div>)}</div><section className="panel"><h2>Recent students</h2><div className="table-scroll"><table><thead><tr><th>Name</th><th>Email</th><th>Gender</th><th>Class</th><th>Added</th></tr></thead><tbody>{s.recentStudents.map(x=><tr key={x.id}><td>{x.first_name} {x.last_name}</td><td>{x.email||'—'}</td><td>{x.gender||'—'}</td><td>{x.class_name||'Unassigned'}</td><td>{new Date(x.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></section></div>}
