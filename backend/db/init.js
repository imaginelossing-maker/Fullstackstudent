import pool from '../config/database.js';
export async function initializeDatabase(){await pool.query(`CREATE TABLE IF NOT EXISTS users(id SERIAL PRIMARY KEY,email VARCHAR(255) UNIQUE NOT NULL,password TEXT NOT NULL,first_name VARCHAR(100) NOT NULL,last_name VARCHAR(100) NOT NULL,role VARCHAR(30) NOT NULL DEFAULT 'TEACHER',is_active BOOLEAN NOT NULL DEFAULT TRUE,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE TABLE IF NOT EXISTS classes(id SERIAL PRIMARY KEY,name VARCHAR(100) UNIQUE NOT NULL,level VARCHAR(100),class_teacher_id INTEGER REFERENCES users(id) ON DELETE SET NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE TABLE IF NOT EXISTS students(id SERIAL PRIMARY KEY,first_name VARCHAR(100) NOT NULL,last_name VARCHAR(100) NOT NULL,email VARCHAR(255),gender VARCHAR(20),class_id INTEGER REFERENCES classes(id) ON DELETE SET NULL,average_score NUMERIC(5,2) NOT NULL DEFAULT 0,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE TABLE IF NOT EXISTS results(id SERIAL PRIMARY KEY,student_id INTEGER NOT NULL REFERENCES students(id) ON DELETE CASCADE,subject VARCHAR(100) NOT NULL,test_score NUMERIC(5,2) NOT NULL CHECK(test_score BETWEEN 0 AND 50),exam_score NUMERIC(5,2) NOT NULL CHECK(exam_score BETWEEN 0 AND 50),total_score NUMERIC(5,2) GENERATED ALWAYS AS (test_score + exam_score) STORED,grade VARCHAR(2) NOT NULL,academic_session VARCHAR(30),term VARCHAR(30),created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE INDEX IF NOT EXISTS idx_students_class ON students(class_id);CREATE INDEX IF NOT EXISTS idx_results_student ON results(student_id);`)}
