import {Router} from 'express';import {getAllClasses,getClassById,createClass,updateClass,deleteClass} from '../controllers/classController.js';import {authenticateToken} from '../middleware/auth.js';const r=Router();r.use(authenticateToken);r.get('/',getAllClasses);r.get('/:id',getClassById);r.post('/',createClass);r.put('/:id',updateClass);r.delete('/:id',deleteClass);export default r;
