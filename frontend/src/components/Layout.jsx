import { NavLink, Outlet, useNavigate } from 'react-router-dom';
import { LayoutDashboard, Users, School, ClipboardList, LogOut, Menu, X } from 'lucide-react';
import { useState } from 'react';
import useAuthStore from '../store/authStore';
export default function Layout(){ const [open,setOpen]=useState(false); const logout=useAuthStore(s=>s.logout); const user=useAuthStore(s=>s.user); const nav=useNavigate(); const links=[['/dashboard','Dashboard',LayoutDashboard],['/students','Students',Users],['/classes','Classes',School],['/results','Results',ClipboardList]]; const signout=()=>{logout();nav('/login')}; return <div className="app-shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand">Student<span>Manager</span></div><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className={({isActive})=>isActive?'active':''}><Icon size={19}/>{label}</NavLink>)}</nav><button className="logout" onClick={signout}><LogOut size={18}/>Sign out</button></aside><div className="main"><header className="topbar"><button className="menu-btn" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button><div><strong>{user?.first_name} {user?.last_name}</strong><span>{user?.role}</span></div></header><main className="content"><Outlet/></main></div></div> }
