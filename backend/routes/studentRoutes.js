import {Router} from 'express';import {getAllStudents,getStudentById,createStudent,updateStudent,deleteStudent} from '../controllers/studentController.js';import {authenticateToken} from '../middleware/auth.js';const r=Router();r.use(authenticateToken);r.get('/',getAllStudents);r.get('/:id',getStudentById);r.post('/',createStudent);r.put('/:id',updateStudent);r.delete('/:id',deleteStudent);export default r;
